name: ankhlens_app
description: An Egyptian hieroglyphics scanning and dictionary application.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  # سيتم إضافة مكتبات الكاميرا وقاعدة البيانات لاحقاً حسب الحاجة

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/
